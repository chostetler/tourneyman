<div class="card mb-3 border-primary" style="max-width: 40rem; margin: 10px">
    <h4 class="card-header text-white bg-primary"> Match {{ match.id }} 
        <span class='border badge rounded-pill bg-primary border-white'> 
            <i class="fa-solid fa-location-dot"></i> {{ match.room.name }} 
            <i class="fa-solid fa-clock"></i>{{ match.start_time|date:"h:m a" }} 
        </span> 
    </h4> 
    <table class="table table-hover">
        <tbody>
            <tr class="table-default">
            {% if match.home_team %}
                <td> {{ match.home_team.name }} </td>
                <td> 
                    {% if match.home_score %}
                        {{ match.home_score }}
                    {% else %} 
                        TBD
                    {% endif %}
                </td>
            {% else %}
                <td> TBD </td>
                <td> TBD </td>
            {% endif %}
            </tr>


            <tr class="table-default">
            {% if match.away_team %}
                <td> {{ match.away_team.name }} </td>
                <td> 
                    {% if match.away_score %}
                        {{ match.away_score }}
                    {% else %} 
                        TBD
                    {% endif %}
                </td>
            {% else %}
                <td> TBD </td>
                <td> TBD </td>
            {% endif %}
            </tr>
        </tbody>
    </table>
</div>
